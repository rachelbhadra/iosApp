# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShopSync' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ShopSync
  pod 'Firebase/Core'
  pod 'FirebaseAuth'
  pod 'FirebaseStorage'
  pod 'SDWebImage'
  pod 'MBProgressHUD'
  pod 'SDWebImage'
  pod 'FirebaseDatabase'

  target 'ShopSyncTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShopSyncUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
